Point de restauration de la version precedente : AxioLoad_0.9.0_original.zip
Pour revenir en arriere, arreter Docker sans supprimer les volumes, extraire la version 0.9.0 puis reconstruire l'image.
Ne pas utiliser docker compose down -v.
